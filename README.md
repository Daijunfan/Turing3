# GermSynth

This repository contains three proof-carrying bilinear-algorithm research
stages:

- `germsynth/`: GermSynth-R exact discovery and regenerative phenotypes.
- `germsynth_fusion/`: GermSynth-F cross-slot fusion extraction audit.
- `germsynth_cr/`: GermSynth-CR source forensics, residual completion, and
  contact-rank compilation.

Current official state: [LATEST_HANDOFF.md](LATEST_HANDOFF.md).

Run the complete current reproduction with:

```bash
./reproduce_contact_repair.sh
```

No PASS is inferred from catalog metadata. Exact Python and independent C++
checks are the correctness boundary.
